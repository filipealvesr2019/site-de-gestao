import React from 'react';
import { BlogComponent } from '../BlogComponent/BlogComponent.';

const BlogPage = () => {
  return (
    <div>
      <BlogComponent />
    </div>
  );
};

export default BlogPage; 